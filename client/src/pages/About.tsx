import PageShell from '@/components/layout/PageShell';
import { Label, Section } from '@/components/ui/primitives';
import AboutLeadership from '@/components/sections/AboutLeadership';
import AboutValues from '@/components/sections/AboutValues';
import { leaders as defaultLeaders } from '@/data/home';
import { useCollection } from '@/lib/siteContent';
export default function About(){const leaders=useCollection('team',defaultLeaders,item=>({name:item.name,role:item.role,metric:item.badge,bio:item.bio,img:item.img}));return <PageShell eyebrow="About CLYX" title={<>Culture creates<br/><span className="text-yellow">the opening.</span></>} intro="CLYX is a performance creative studio for brands that want to move faster than the feed. We connect creator instinct, paid distribution, and the systems that make growth repeatable."><Section><div className="split-intro grid gap-10 md:grid-cols-2 md:items-start"><div><Label>Built for the brave</Label><h2 className="display mt-5 max-w-2xl text-4xl font-bold md:text-6xl">The ad should feel like culture. The result should feel like math.</h2></div><p className="max-w-xl text-base leading-7 text-muted md:pt-10">Most agencies choose between creative and performance. We do not. CLYX connects the instinct that makes people stop with the systems that make brands grow.</p></div></Section><AboutValues/><AboutLeadership members={leaders}/></PageShell>}
