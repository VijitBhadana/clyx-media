import { ArrowUpRight } from 'lucide-react';
import PageShell from '@/components/layout/PageShell';
import { Label, Section } from '@/components/ui/primitives';
import { useCollection } from '@/lib/siteContent';
const defaultPosts=[{date:'12.09.25',tag:'Creator economy',title:'Why the best creator ads do not feel like ads',color:'bg-yellow text-dark'},{date:'04.09.25',tag:'Performance',title:'The performance creative loop, explained',color:'bg-blue text-white'},{date:'28.08.25',tag:'Growth',title:'From scroll-stopping hook to scalable system',color:'blog-soft-card text-dark'},{date:'19.08.25',tag:'CRO',title:'The page is part of the ad',color:'bg-yellow text-dark'},{date:'11.08.25',tag:'Strategy',title:'What to do when everything is working a little',color:'bg-blue text-white'},{date:'02.08.25',tag:'Creators',title:'Briefing for a voice, not a demographic',color:'blog-soft-card text-dark'}];
const blogStyles:Record<string,string>={yellow:'bg-yellow text-dark',blue:'bg-blue text-white',soft:'blog-soft-card text-dark'};
export default function Blog(){const posts=useCollection('blog',defaultPosts,item=>({date:item.date,tag:item.tag,title:item.title,color:blogStyles[item.style]??blogStyles.yellow}));return <PageShell eyebrow="Journal" title={<>Ideas that<br/><span className="text-yellow">move.</span></>} intro="Notes on creator culture, performance creative, conversion, and the systems that turn attention into momentum."><Section><div className="grid gap-4 sm:grid-cols-2 md:grid-cols-3">{posts.map((post,i)=><article key={`${post.title}-${i}`} className={`min-h-[280px] rounded-xl p-6 ${post.color} transition-transform hover:-translate-y-1`}><div className="flex justify-between"><p className="font-mono text-xs opacity-65">{post.date}</p><ArrowUpRight size={18}/></div><div className="mt-20"><p className="text-[10px] font-semibold uppercase tracking-[.14em] opacity-65">{post.tag}</p><h2 className="display mt-3 text-3xl font-semibold">{post.title}</h2></div></article>)}</div></Section><section className="bg-blue text-white"><div className="container flex flex-col gap-8 py-20 md:flex-row md:items-end md:justify-between md:py-28"><div><Label style={{color:'#FFDE59'}}>The CLYX dispatch</Label><h2 className="display text-5xl font-bold md:text-7xl">Keep your edge.</h2></div><a href="/contact" className="inline-flex items-center gap-3 rounded-xl bg-yellow px-6 py-4 text-sm font-semibold uppercase tracking-[.1em] text-dark">Subscribe to the dispatch <ArrowUpRight size={16}/></a></div></section></PageShell>}
