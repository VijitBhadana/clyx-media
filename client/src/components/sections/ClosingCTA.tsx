import { Button, Label } from '@/components/ui/primitives';
import { Reveal, useDirectionalReveal } from '@/components/ui/ScrollMotion';
export default function ClosingCTA(){const left=useDirectionalReveal<HTMLSpanElement>(), right=useDirectionalReveal<HTMLSpanElement>();return <section className="closing-cta bg-yellow py-24 text-dark md:py-36"><div className="container grid gap-10 md:grid-cols-[1.2fr_.8fr] md:items-end"><div><Label>Ready when you are</Label><h2 className="display max-w-4xl text-6xl font-bold md:text-8xl"><span ref={left.ref} className={`direction-reveal from-left block ${left.visible?'is-visible':''}`}>Make your next</span><span ref={right.ref} className={`direction-reveal from-right block text-blue ${right.visible?'is-visible':''}`}>move louder.</span></h2></div><Reveal delay={120}><p className="max-w-sm text-lg leading-7">Tell us what you're building, what's stuck, and where you want to go next.</p><Button href="#contact" className="mt-8 bg-dark !text-white hover:bg-blue">Start a project</Button></Reveal></div></section>}
