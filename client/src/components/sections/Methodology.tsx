import { ArrowUpRight } from 'lucide-react';
import { methodologySteps } from '@/data/home';
import { Reveal } from '@/components/ui/ScrollMotion';
import { Label, Section } from '@/components/ui/primitives';

export default function Methodology(){return <Section id="methodology" className="bg-blue text-white"><div className="grid gap-14 md:grid-cols-[.9fr_1.1fr]"><Reveal><Label style={{color:'#FFDE59'}}>The CLYX methodology</Label><h2 className="display max-w-xl text-5xl font-bold md:text-7xl">A one-off post<br/><span className="text-yellow">doesn't sell.</span></h2><p className="mt-8 max-w-md text-base leading-7 text-white/75">A whitelisted ad, run on data, does. We turn creator content into paid media through the creator's own handle so it reads as a genuine recommendation, not a sponsored pitch.</p><a href="#contact" className="mt-8 inline-flex items-center gap-2 border-b border-yellow pb-2 text-sm font-semibold uppercase tracking-[.1em] text-yellow">Talk strategy <ArrowUpRight size={15}/></a></Reveal><div className="border-t border-white/25">{methodologySteps.map((step,i)=><Reveal key={step.index} delay={i*260}><div className="grid gap-4 border-b border-white/25 py-8 md:grid-cols-[60px_1fr]"><span className="font-mono text-sm text-yellow">{step.index}</span><div><h3 className="display text-2xl font-semibold md:text-4xl">{step.title}</h3><p className="mt-3 max-w-md text-sm leading-6 text-white/70">{step.text}</p></div></div></Reveal>)}</div></div></Section>}
