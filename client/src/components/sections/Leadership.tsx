import { ArrowUpRight } from 'lucide-react';
import { leaders } from '@/data/home';
import { Reveal } from '@/components/ui/ScrollMotion';
import { Label, Section } from '@/components/ui/primitives';
import { useCmsRecord } from '@/lib/cms';

export default function Leadership(){const cms=useCmsRecord('homepage','leadership').record?.content??{};const heading=String(cms.heading??'Small team. Direct founder access.');const team=Array.isArray(cms.leaders)?cms.leaders as typeof leaders:leaders;return <Section id="leadership" className="bg-[color:var(--background)]"><div className="mb-14 grid gap-8 md:grid-cols-[1.35fr_.65fr] md:items-start"><Reveal><Label>{String(cms.eyebrow??'CLYX leadership')}</Label><h2 className="display text-4xl font-bold md:text-6xl lg:text-7xl">{heading.split(/\s+(?=Direct founder access\.?)/i).map((line,i)=><span key={i} className={i?'block text-blue':'block'}>{line}</span>)}</h2></Reveal><Reveal delay={420}><p className="max-w-sm pt-16 text-sm leading-6 text-muted">{String(cms.intro??'You collaborate directly with senior partners who have scaled eight-figure ad spend across high-growth categories.')}</p></Reveal></div><div className="grid border-t border-grid md:grid-cols-3">{team.map((leader,i)=><Reveal key={leader.name} delay={i*260}><article className={`group border-b border-grid py-8 md:border-b-0 md:pr-8 ${i>0?'md:border-l md:pl-8':''}`}><div className="flex justify-end"><ArrowUpRight className="text-blue transition-transform group-hover:translate-x-1 group-hover:-translate-y-1" size={20}/></div><div className="mt-8"><h3 className="display text-3xl font-semibold md:text-4xl">{leader.name}</h3><p className="mt-2 text-xs font-semibold uppercase tracking-[.12em] text-muted">{leader.role}</p><p className="mt-4 text-sm leading-6 text-muted">{leader.bio}</p><a href="#contact" className="mt-5 inline-flex items-center gap-2 text-xs font-semibold uppercase tracking-[.12em] text-blue">Direct advisory <ArrowUpRight size={14}/></a></div></article></Reveal>)}</div></Section>}
