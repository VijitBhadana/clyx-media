# Vercel -> Project Settings -> Environment Variables (not needed for local dev; Vite proxies /api to :3000).
# Full URL of the Render backend, no trailing slash.
VITE_API_URL=https://clyx-media-backend.onrender.com
